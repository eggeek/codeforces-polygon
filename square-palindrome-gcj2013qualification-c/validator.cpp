#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;
typedef long long ll;
const ll maxn = 100000000000000;
ll a, b;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    a = inf.readLong(1, maxn, "L");
    inf.readSpace();
    b = inf.readLong(1, maxn, "R");
    inf.readEoln();
    inf.readEof();
}
