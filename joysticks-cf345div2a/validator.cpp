#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;
const int maxn = 100;
int a, b;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    a = inf.readInt(1, maxn, "a");
    inf.readSpace();
    b = inf.readInt(1, maxn, "b");
    inf.readEoln();
    inf.readEof();
}
