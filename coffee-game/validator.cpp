#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;
int n;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    n = inf.readInt(1, 3000, "n");
    inf.readEoln();
    inf.readEof();
}
