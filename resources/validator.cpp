#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    int n = inf.readInt(2, 100, "n");
    inf.readSpace();
    inf.readInt(2, min(n, 26), "k");
    inf.readEoln();
    inf.readEof();
}
